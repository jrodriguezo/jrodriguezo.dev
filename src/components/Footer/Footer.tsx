import React from "react";
import styles from "@/components/Footer/Footer.module.css";

function Footer() {
  return <footer className={styles.footerWrapper}>footer</footer>;
}

export default Footer;
